name: Hello workflow

on: push

jobs:
  hello-step1:
    runs-on: ubuntu-latest

    steps:
      - name: step1
        run: echo "first job"

  hello-step2:
    runs-on: ubuntu-latest
 
    needs: hello-step1

    steps:
      - name: step2
        run: echo "second job"
